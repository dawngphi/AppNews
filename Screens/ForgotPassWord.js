import { Image, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ForgotPassWord = () => {
  return (
    <View style={forgotStyles.body}>
        
    </View>
  )
}

export default ForgotPassWord

const forgotStyles = StyleSheet.create({
    body: {
        padding: 24,
        backgroundColor: 'red',
        width: '100%',
        height: '100%'
    },
})